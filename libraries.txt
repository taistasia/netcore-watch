Adafruit GFX Library
Adafruit ILI9341
SdFat - Adafruit Fork
# Wire (built-in ESP32 Arduino) — used by svc_air.cpp for I2C (real hardware mode)
# Note: AIR_DEMO_MODE=1 (default) uses no external I2C library.
# For real SCD40 hardware: ensure Wire.h is available (always included in ESP32 core).
# MLX90614 — NO external library needed.
# svc_tempir.cpp drives the MLX90614 via raw Wire (SMBus read).
# TEMPIR_DEMO_MODE=1 (default): no hardware required.
# TEMPIR_ENABLED=0: entire module compiles as no-ops.
# MFRC522 by miguelbalboa — required ONLY when RFID_DEMO_MODE 0
# Install via Arduino Library Manager: search "MFRC522"
# RFID_DEMO_MODE=1 (default): no library needed, Wokwi-safe.
# RFID_ENABLED=0: no library needed, full no-op stubs.
